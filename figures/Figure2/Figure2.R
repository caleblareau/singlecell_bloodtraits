library(ggplot2)
library(BuenColors)

cellCoordsDF <- data.frame(
  CellLabel = c("HSC", "MPP", "LMPP", "CLP", "GMP1", "GMP2", "GMP3", "CMP", "MEP", "NK", "CD4", "CD8", "B", "pDC", "Mono", "Ery")
          x = c(), 
          y = c(10       8,      7,     5,     6,        5,     5,     7,     5,    2,     2,     2,     2, )
)